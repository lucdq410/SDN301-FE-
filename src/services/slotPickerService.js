import { useState } from "react";
import axios from "axios";
import { toast } from "react-toastify";
import { apiURL } from "../configs";
const slotPickerService = () => {
  const api = apiURL + "/slotPicker";
  return {};
};

export default slotPickerService;
